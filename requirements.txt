# Runtime dependencies — everything the poller needs to run in production.
# Direct dependencies only; pip resolves the transitive ones (certifi, idna,
# urllib3, charset-normalizer) itself.

psycopg2-binary==2.9.12
python-dotenv==1.2.3
requests==2.34.2
