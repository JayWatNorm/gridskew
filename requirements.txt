# Direct dependencies only. Pin these once the environment is working:
#   pip freeze > requirements.txt
# NOTE for PowerShell: `pip freeze > file` writes UTF-16 with a BOM on
# Windows PowerShell 5.1, which pip cannot then read back. Use
#   pip freeze | Out-File -Encoding utf8 requirements.txt
# (UK-Crime-Pipeline/requirements.txt currently carries that BOM.)

requests
psycopg2-binary
python-dotenv
pytest
